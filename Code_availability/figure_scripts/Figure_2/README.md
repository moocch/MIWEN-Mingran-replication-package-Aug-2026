# Fig. 2 — Physics-informed machine-learning digital twin of the analog compute element

File used by the tex: `Data_availability/Figure_2/fig2_v3_preview.pdf` (copy here). All 11 panels (a–k)
are generated by **one script, `fig2_v3_twin.py`**; a copy sits in every panel folder.
Reproduce here: `python fig2_v3_twin.py` (reads `../Data_availability/Figure_3/` and `data/` shims).

**2026-08-28/29 restructure (author, script edited in place):** the former panel a
(twin-architecture schematic) was **removed from the figure** — the twin's two-block
construction is now described in the Results text — and the remaining panels were
relettered **a–k** (old b–l). This archive was restructured to match: the old `a/`
folder (schematic, no data) was removed, the panel folders were shifted one letter
down, and the script (all 12 copies) and the four `fig2_v3_preview.*` files were
refreshed. **Re-executed from this folder's archived data on 2026-08-29: the
regenerated PNG is pixel-identical to the published one (0 / 13,552,188 differing
pixels).** All six data inputs (`../Data_availability/Figure_3/twin_predictions_N{1,2,4,8,4096}.npz`,
`data/N4096_cascade_raw_data.npz`) are MD5-unchanged since the 2026-08-28 audit, so
every measured-data provenance chain in the panel folders remains valid.
(Earlier 2026-08-28 state — 12 panels with a redesigned schematic panel a — was
also execution-verified pixel-identical, 0 / 17,067,765, before the removal.)

| Panel | Content | Direct data |
|---|---|---|
| a | N=1 measured vs twin surface, RMSE 1.30 dB | twin_predictions_N1.npz |
| b/c/d | N=2/4/8, RMSE 1.91/1.67/1.55 dB | twin_predictions_N{2,4,8}.npz |
| e | N=4096, RMSE 0.79 dB | twin_predictions_N4096.npz |
| f/g/h/i | N=4096 measured / ideal (10.35 dB) / physics-only (1.80 dB) / full twin (0.79 dB) | N4096_cascade_raw_data.npz |
| j | RMSE vs vector length, three model tiers | panel_k_values.csv (in `j/`) |
| k | error-reduction factors per N | panel_l_values.csv (in `k/`) |

(The two summary CSVs keep their original file names from the previous lettering;
their content is unchanged and matches the script's hard-coded published values.)

Three measurement chains:
1. **N=1 scalar** (a): USRP X310 dual-channel → ZEM-4300 → N9020A readout,
   43×43 grid (−70…+14 dBm, 2 dB, 3 repeats) → heatmap_2db.npz → scalar PIML twin.
2. **N=2/4/8 vector** (b–d): 33×33 grids, N9020A readout → usrp_mixer_vector_heatmap_N*.npz
   → vector twin (physics frozen).
3. **N=4096 all-USRP** (e–i): dual-USRP chain (uncalibrated amplitude; validates the
   response shape) → gr_usrp_mixer_vector_heatmap_N4096.npz → N4096 twin;
   the cascade npz of f–i is its verified repackaging.
