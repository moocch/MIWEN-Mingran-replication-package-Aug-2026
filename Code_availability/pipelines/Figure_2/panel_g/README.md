# Fig. 2g — ideal-multiplier surface at N=4096, RMSE 10.35 dB

Same data file and chain as `..\g\README.md`; this panel plots
`perfect_multiplier_db` (ideal P_IF = P_LO + P_RF + const; model surface with a
single overall offset), RMSE = `error_metrics[0,0]` = 10.35 dB.
Plot code: `fig2_v3_twin.py`, "g–j" section.
